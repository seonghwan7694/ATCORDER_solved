#include <bits/stdc++.h>
using namespace std;


int N, M;
pair<int, int> A[200001];
int dp[2020202];
int main(){
  ios::sync_with_stdio(false);
  cin.tie(0); cout.tie(0)

  cin >> N >> M;
  for(int i = 1; i <= N; i++){
    cin >> A[i].first >> A[i].second;
  }

  return 0;
}